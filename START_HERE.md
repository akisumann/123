# はじめに読む（START HERE）— AI 向け入口

このリポジトリは TRPG 世界「**123**」の設定データベース兼・正典です（137 ファイル / 約 42 万文字）。
**このファイルは、どの AI で使うときも「最初に読む 1 枚目」**として用意しています。

## いちばん大事なこと：AI の能力に合わせて渡し方を変える

このリポジトリは**分割ファイル前提**の設計（場面ごとに必要なファイルだけを読む）です。
渡し方は AI の能力によって 3 通りあり、**上ほど読み込み精度が高い**。

| 渡す物 | 向いている AI | 中身 |
|---|---|---|
| **zip（＋個別ファイル）** | zip を展開してファイルを開ける AI | 分割されたまま。`INDEX.md` を地図に必要ファイルだけ読む（最良） |
| **`123_all.md`** | **zip を読めない AI** | 全ファイルを 1 枚に結合。ただし**先頭に正典＋目次**を配置済みで読み落ちにくい |
| **`DIGEST.md`** | 42 万字すら重い／核だけ先に渡したい時 | 入口＋運用ルール＋目次だけ（約 2 万字）。詳細は後から個別に貼る |

> `123_all.md` を「読み込んでくれない」対策の肝は、**先頭に圧縮版の正典と目次を置く**ことです。
> AI は長文でも冒頭は必ず読むため、重要な核が最初にあれば途中が薄くなっても破綻しにくい。
> それでも、可能なら zip＋個別ファイル（または `DIGEST`＋必要分）の方が精度は上です。

### AI の種類ごとの使い方

- **ファイル検索できる AI（Claude Code 等）**:
  `CLAUDE.md` の「検索プロトコル」に従い、場面が動くたびに該当ファイルを自分で開く。`INDEX.md` を地図に使う。
- **zip を読めない AI（チャットに貼る等）**:
  `123_all.md` を 1 つ渡す。先頭の正典と目次を読ませ、詳細は目次から該当セクションを引かせる。
- **軽く始めたい／核だけ先に渡す**:
  `DIGEST.md` を貼り、詳細が要る場面で `INDEX.md` から該当ファイルだけ追加で貼る。

---

## 圧縮版・正典（これだけで世界の骨格が分かる）

> 詳細は各ファイルにある。以下は「毎回頭に入れておくべき確定事実」だけを抜いた要約。
> 迷ったら詳細ファイル（`INDEX.md` 参照）を開く。矛盾したら詳細ファイルが正。

### 世界

- レベル 1〜100 のハイファンタジー。強さ＝**レベル＋ステータス＋スキル**が基準
  （補助要素：魔法・装備・経験・戦術など）。`world/00_core_concept.md`
- 舞台は**アルヴェイン大陸**。神代に住み着いた**五龍**（大地・火山・氷結・海洋・森林、いずれも Lv90 台）
  の魔力が五つの地域と環境を作り、そこに五大国が成立した。`world/03_history.md`, `world/dragons/`

### 主舞台：クロスロード

- **セントラル・ヘイヴン王国**の中規模交易都市（定住人口 約3.5万＋流動人口）。街道・農村・
  小国家の交易路が交差する要衝。多種族・多文化で余所者に寛容。`world/crossroad/11_crossroad_city.md`
- **5 区画**：中央区(行政)／北区(商業)／東区(住民生活)／南区(職人・工房)／西区(歓楽・闘技)。
  `world/crossroad/20_crossroad_city_districts.md`
- **統治構造**（指揮命令ではなく分野別の分担、全体調整をクラリスが担う）:
  - **クラリス・ヴァイスフェルト**（領主／街全体の方針・調整）`characters/npcs/01`
  - **ミレーヌ・ヴァイスフェルト**（西区・歓楽街トップ／クラリスの実妹・生き別れ再会）`characters/npcs/02`
  - **エイダ・ロックウェル**（衛兵隊長／都市内の治安）`characters/npcs/04`
  - **ヴァレリア・グレンツ**（領主騎士団長／街道・都市外の防衛。通り名「特大剣の騎士団長」）`characters/npcs/03`
  - **ダリオ・ラングフォード**（冒険者ギルド支部長／冒険者と依頼の管理）`characters/npcs/07`

### 街の背景（基調の温度感）

- 数年前、**①ミレーヌの西区正常化 ②黒針会を統制下へ ③クラリスとミレーヌが姉妹と判明**が
  ほぼ同時に重なり、街の連帯感が一気に高まった転機があった。
- 現在のデフォルトの温度感は「**日々の暮らしの裏で、また何かがゆっくり動き始めている**」。
  派手な大事件を急に起こさず、伏線は小出し。`world/crossroad/11_crossroad_city.md`

### 裏社会：黒針会

- 街の裏に薄く広がる**情報・暗殺組織**。ミレーヌの配下筋（放し飼いに近い）。
  会主**ギデオン**、幹部は潜入の**ミーナ**（耳）／監視鳥の**飛鳥**（目）／蠍尾の**ザラ**（刃）。
  情報屋網が住民の生活インフラとして公認利用されている。`world/crossroad/32_black_needle_society.md`

### 冒険者と周辺

- ギルド登録は約1,500人で D〜C 級が中核。**Lv50 超は稼ぎ場を求めて街を出ていく**ため精鋭は少数。
- **危険地域4**：さざめき平原(Lv10-20)／赤牙森林(Lv25-35)／灰岩峡谷(Lv40-50)／骨鳴り墓原(Lv55-65)。
- **ダンジョン3**：黒硝子遺跡(全30階)／忘れられた鉱山(全40階)／星喰いの地下神殿(第50層まで確認)。
  `world/crossroad/11_crossroad_city.md`, `world/14_adventurers_guild.md`

### NPC

- 主要 NPC は **60 人**。全員に「〇〇の[名前]」形式の**通り名**があり、街で会うと役割が一目で分かる。
  一覧（本名・所属つき）は `world/crossroad/44_crossroad_nicknames.md`。個別詳細は `characters/npcs/`。

### ルール（判定）

- ステータス：HP / MP / ATK / DEF / INT / SPD / DEX、ランク S(25)〜F(-4)。`rules/02_status_system.md`
- 戦闘判定：**「スキルLv d ステータスランク」でダイスを振る**（例：剣術Lv4×ATK:B → 4d5）。
  `rules/03_combat_system.md`。魔法の階位は `rules/05_magic_theory.md`。

### 進行方式：観覧モード

- GM がクロスロードの日常・出来事を描写し続け、ユーザーは眺める立場。特別な準備は不要。`CLAUDE.md`

---

## GM が特に外しやすいルール（`CLAUDE.md` に詳細）

1. **能力が絡む行動は、必ず該当 `characters/npcs/` を開いてステータス・スキルを確認**してから描写する。
2. **戦闘の計算を省略しない**。毎ターン実際にダイスを振り、数値を出してから描写する。
3. **登場人物は 8 割方、既存 NPC の再登場で賄う**。名前だけの使い捨てを新造しない。
4. **場面が動いたら該当ディレクトリを能動的に検索・参照**する（記憶や雰囲気で進めない）。
5. **その場の者同士が面識を得るまで名前を出さない**。外見・役割の印象で描写する。
6. セリフは必ず `話者「セリフ」` 形式。

## 索引・ツール

- 各ファイル冒頭の **`> **TL;DR:**` 行** … その 1 行でファイルの中身が分かる。目次や全部載せを
  読むだけで概要を掴め、必要なファイルだけ開けばよい。元データは `tools/summaries.tsv`。
- `INDEX.md` … 全ファイルの地図（TL;DR ＋文字数）。
- `world/crossroad/72_place_character_map.md` … **場所→そこにいるキャラ**の対応マップ。区画・施設へ入った時「誰がいるか」を速攻参照。通り名（速攻参照キー）付き。
- `tools/day_plan.py` … **街の配置生成**。日付と時間帯を入れると、住人 60 人の生活ルーティン
  （`tools/routines.tsv`）＋重み付き乱数で「今どこに誰がいるか」が決まる。GM が都合で人を選ぶ前に、
  街が勝手に動いている状態を先に作るためのもの。住人は**不動**（店主・門衛・受付など持ち場を離れず、
  行けば必ず会える）／**定住**（拠点はあるが時々ふらつく。`※`印＝その人にしては珍しい場所＝場面の種）／
  **遊動**（衛兵隊長・連絡役・情報屋・盗人など一日中うろつく。`→区画`で回る先も出る）に分かれる。
  日付からは暦（年・月・旬）・年中行事・空模様も同時に決まり、祭りの日は人が祭り区域へ集まり、
  荒天の日は門が制限されて遠出が減る。冒険者は行き先の遠さに応じて泊まりがけで街を空ける。
  末尾の**【今日のめぐり合わせ】**は、依頼先が被った・珍しい場所で知人と鉢合わせた偶然の一覧＝そのまま場面の起点。
  例：`python3 tools/day_plan.py --day 3 --time 宵` / `--place 東区` / `--who ミルカ`
- `tools/quest_board.py` … **その日のギルド掲示板**。依頼はその場で考えず、既存の依頼表（`64・65・66`）の
  掲示例から日付固定の乱数で選ぶ。報酬額も表の値なので `world/06_economy.md` の相場を外さない。
  未受注の依頼は数日貼られ続け、現地に出ている顔ぶれも併記される。例：`python3 tools/quest_board.py --day 8`
- `tools/street_talk.py` … **その日、街で流れている噂**。噂の種は実際に起きたこと（誰が発った・戻った、
  珍しい場所で見かけた、高ランク依頼が貼られた、荒天・祭り）から機械的に拾う。噂は日を追って広まり、
  悪天候の日は情報網の伝達が遅れる（`51`）。裏稼業の人物の動きは噂に上がらない。
  例：`python3 tools/street_talk.py --day 8 --place 東区`
- `tools/scene_context.py` … **GM 進行用シーンパック生成**。場面の入力（人物・場所・時間帯）から
  必要な canon（ステータス・スキル・口調・行きつけ・その場の顔ぶれ）を機械抽出して 1 ブロックで出力。
  記憶や雰囲気で進行せず、**各場面の前にこれを実行し、出力を読んでから描写する**。
  例：`python3 tools/scene_context.py --chars 天雷,ツバキ --place 夜鴉の止まり木 --time 宵`
- `tools/generate_character_stats.py` / `tools/check_character_stats.py` … 新キャラのステータスを
  コードで生成・検算（AI が数値を雰囲気で決めない。`rules/10_new_character_format.md`）。
- `tools/apply_summaries.py` … `summaries.tsv` を各ファイル冒頭へ挿入（冪等）。
- `tools/make_all.py` … zip 非対応 AI 用の `123_all.md`（先頭に正典＋目次）を生成。
- `tools/make_digest.py` … 核だけの `DIGEST.md`（約 2 万字）を生成。
- `tools/count.py` … 文字数計測（`--budget` で入口が肥大していないか警告）。

> **ファイルを追加・編集したら `bash tools/build.sh` を実行**すれば、TL;DR 挿入・索引・全部載せ・
> ダイジェスト・zip がすべて最新に揃う。新規ファイルを足したら `summaries.tsv` に 1 行追記する。
